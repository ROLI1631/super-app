# Shared

Shared contains reusable contracts, helpers, and types that support both engines and modules without embedding business logic.

## Purpose

- Shared platform contracts that depend on Core.
- Common abstractions used by multiple modules or engines.
- Data shapes and validation helpers that are not module-specific.
